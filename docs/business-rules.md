# OmnyReporter - Бизнес правила

## Что такое OmnyReporter?

OmnyReporter — это **единая платформа анализа качества кода и тестирования**, которая объединяет результаты анализа статического кода (ESLint, TypeScript) в едином инструменте с целью дать разработчикам полный контроль над качеством кода.

---

## Основные правила для пользователя

### 1. Диагностика кода

#### 1.1. Сбор диагностики

- OmnyReporter **собирает** проблемы из нескольких источников (инструментов анализа):
    - ESLint (правила для JavaScript/TypeScript)
    - TypeScript (ошибки типизации)
    - Vitest (результаты тестирования)
- Все источники работают **параллельно** для максимальной скорости
- Если один источник недоступен или ошибается, остальные продолжают работу

#### 1.2. Форматы вывода диагностики

- **JSON** — машиночитаемый формат для интеграции с другими системами (CI/CD, IDE)
- **Pretty** — красивый вывод в консоль с цветами и иконками
- **Table** — таблица с сортировкой и выравниванием для быстрого просмотра
- Пользователь выбирает формат через флаг `--format`

#### 1.3. Фильтрация диагностики

- По **шаблонам файлов** (например: `src/**/*.ts`, `tests/**/*.test.ts`)
- По **источникам** (--eslint, --typescript, --vitest)
- По **корневой директории** проекта

### 2. Статистика и метрики

#### 2.1. Автоматический расчёт статистики

- **По типам проблем**: количество ошибок, предупреждений, информационных сообщений
- **По файлам**: какие файлы содержат больше всего проблем
- **По кодам ошибок**: какие правила срабатывают чаще всего
- Статистика расчитывается **автоматически** при каждом сборе диагностики

#### 2.2. Группировка результатов

- Все диагностики группируются по исходному источнику для ясности
- В статистике видна разбивка по severity уровням (error, warning, info, note)

### 3. Интеграция и использование в CI/CD

#### 3.1. CLI интерфейс

- OmnyReporter работает как **команда** из консоли: `omnyreporter diagnostics`
- Может запускаться в CI/CD pipeline (GitHub Actions, GitLab CI, Jenkins и т.д.)
- Возвращает **код выхода** (exit code) для обработки в pipeline

#### 3.2. Программное использование

- Может использоваться как **Node.js модуль** для встраивания в другие инструменты
- Экспортирует **типизированный API** для TypeScript приложений
- Все результаты типизированы через TypeScript

#### 3.3. Вывод результатов

- Результаты можно **сохранять в файл** (JSON, HTML и т.д.)
- Можно выводить в **stdout** для обработки другими инструментами
- Интеграция с **Slack, Teams, email** через системы уведомлений

### 4. Обработка ошибок

#### 4.1. Гарантии обработки

- Если анализ **одного источника** упал (например, ESLint), остальные источники продолжат работу
- Пользователю будет **явно указано**, какие источники были успешны, а какие нет
- Результаты будут **полные** даже при частичных ошибках

#### 4.2. Логирование

- Все ошибки логируются с **достаточным контекстом** для отладки
- Флаг `--verbose` включает детальное логирование для отладки
- Чувствительные данные (пути, токены) **автоматически скрываются** в логах

### 5. Производительность и масштабируемость

#### 5.1. Параллельный анализ

- Несколько источников анализа работают **одновременно**, не ожидая друг друга
- Поддержка **concurrency** — контроль количества одновременных операций

### 6. Расширяемость и гибкость

#### 6.1. Добавление новых источников

- Архитектура позволяет легко добавлять **новые источники диагностики** (например, Semgrep, SonarQube API)
- Каждый новый источник должен реализовать **один интерфейс**
- Остальной код **не требует изменений**

#### 6.2. Кастомные форматтеры

- Пользователи могут добавлять **собственные форматеры вывода**
- Это позволяет интегрировать с **любыми внешними системами**

#### 6.3. Плагины и расширения

- Система построена на **инверсии зависимостей** для поддержки расширений
- Будущие версии предусмотрят **SDK для плагинов**

---

## Внутренние правила (для понимания поведения)

### 7. Архитектурные принципы, которые влияют на поведение

#### 7.1. Слоистая архитектура

- Код разделён на **7 слоёв** с чёткими границами
- Каждый слой может быть **независимо тестирован** и **заменён**
- Внешние зависимости изолированы в **infrastructure слое**

#### 7.2. Контракты и интерфейсы

- Все компоненты общаются через **интерфейсы** (contracts)
- Это означает, что реализация может меняться, а потребители не будут затронуты
- Логирование, файловая система, форматирование — всё через интерфейсы

#### 7.3. Result типы (для обработки ошибок)

- Вместо выброса исключений используется **Result<T, E>** тип
- Это означает: каждый результат либо **успешен** (Ok), либо содержит **ошибку** (Err)
- Это делает обработку ошибок **явной** и **безопасной**

#### 7.4. Dependency Injection

- Все зависимости **внедряются** через конструктор
- Это позволяет легко **подменять реализации** на тесты
- IoC контейнер автоматически собирает все зависимости

### 8. Сбор и агрегация данных

#### 8.1. Процесс сбора

1. **Очистка** — удаляются старые результаты (если нужно)
2. **Сбор** — параллельный запуск всех источников диагностики
3. **Агрегация** — объединение результатов в единый список
4. **Аналитика** — расчёт статистики
5. **Запись** — сохранение результатов в файл/stdout

#### 8.2. Нормализация данных

- Все диагностики из разных источников преобразуются в **единый формат**
- Каждая диагностика имеет **уникальный ID**, **строку, колонку, severity, сообщение**
- Это позволяет **корреляции** между диагностиками из разных инструментов (в будущем)

### 9. Валидация конфигурации

#### 9.1. Обязательные параметры

- **rootPath** — корневая директория проекта (обязательна)
- **patterns** — шаблоны файлов для анализа (если не указаны, по умолчанию `.`)

#### 9.2. Опциональные параметры

- **configPath** — путь к пользовательскому конфигу
- **ignorePatterns** — шаблоны файлов, которые игнорировать
- **cache** — включить кеширование результатов
- **timeout** — таймаут для каждого источника
- **concurrency** — количество одновременных операций
- **verboseLogging** — детальное логирование

#### 9.3. Валидация происходит с помощью zod

- Если конфигурация **неверна**, она будет **явно отклонена** до запуска анализа
- Ошибки валидации **информативны** и указывают на конкретные проблемы

---

## Примеры использования

### Пример 1: Базовый запуск

```bash
omnyreporter diagnostics
```

- Анализирует текущую директорию (`.`)
- Использует все доступные источники (ESLint, TypeScript, Vitest)
- Выводит результаты в формате `pretty` (по умолчанию)

### Пример 2: Анализ конкретных файлов

```bash
omnyreporter diagnostics "src/**/*.ts" "tests/**/*.test.ts"
```

- Анализирует только TypeScript файлы в `src/` и `tests/`
- Остальные файлы игнорируются

### Пример 3: JSON вывод для интеграции

```bash
omnyreporter diagnostics --format json --output report.json
```

- Выводит результаты в JSON
- Сохраняет в файл `report.json`
- Удобно для обработки другими инструментами

### Пример 4: Отключение некоторых источников

```bash
omnyreporter diagnostics --eslint --no-typescript
```

- Запускает только ESLint
- Отключает TypeScript анализ

### Пример 5: Детальное логирование для отладки

```bash
omnyreporter diagnostics --verbose
```

- Выводит все промежуточные шаги
- Помогает понять, почему что-то не работает

---

## Гарантии качества

### 10. Обещания, которые даёт OmnyReporter

- ✅ **Точность**: Все результаты из исходных инструментов передаются без изменений (только нормализация)
- ✅ **Скорость**: Параллельный анализ быстрее, чем последовательный запуск инструментов
- ✅ **Надёжность**: Ошибка в одном источнике не блокирует результаты других
- ✅ **Типизация**: Полная поддержка TypeScript — все API типизированы
- ✅ **Расширяемость**: Лёгко добавлять новые источники и форматеры
- ✅ **Безопасность**: Чувствительные данные скрываются в логах, поддержка кастомных санитайзеров

---

## Будущие возможности (Roadmap)

- 📊 **Визуализация**: HTML dashboard с историей проблем
- 🔗 **Корреляция**: Связь между падениями тестов и изменениями кода
- 🤖 **AI/ML**: Автоматическое определение flaky тестов, рекомендации по исправлениям
- 🔌 **Плагины**: SDK для написания собственных источников и форматеров
- ☁️ **Cloud**: SaaS версия с multi-project поддержкой
- 📱 **Интеграции**: Slack, Teams, Jira, GitHub, GitLab и др.
